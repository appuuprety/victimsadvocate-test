[pytest]
testpaths = tests
addopts = -v --tb=short --strict-markers
markers =
    ui: UI tests using Playwright
    api: API tests using requests
    admin: tests that require admin login
    smoke: quick sanity checks (run on every push)
    integration: cross-system tests (API + key UI flows)
    regression: full suite (run on schedule)
